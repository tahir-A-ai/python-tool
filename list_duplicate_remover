def remove_duplicates(lst):
    seen = []
    result = []
    
    for item in lst:
        if item not in seen:
            seen.append(item)
            result.append(item)
    
    return result

lst = [3,4,2,1,3,4,6]
print(remove_duplicates(lst))